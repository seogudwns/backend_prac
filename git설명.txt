git init
git add .
git commit -m "하고싶은말"
git remote add origin 원격주소
git push     -->      #처음에는 git push --set-upstream origin master 를 쳐야할 수 있음.


npm init     #package.json설치.
(main은 주로 app.js, auth는 내 이름.)
npm i express    #express를 사용하기 위해 설치.

까먹지 말 것!
================
app.get('/',(req,res)=>{~~~})

app.listen([포트번호, 혹은 주소등],(함수))
================

package.json에 추가.. scripts 부분 --> "start":'nodemon app' # 사용할것.
+npm i nodemon

